[project]
name = "usao-ct-scraper"
version = "0.1.0"
description = "Scraper for DOJ District of Connecticut press releases"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.40",
]

[project.scripts]
scrape = "scraper:main"
